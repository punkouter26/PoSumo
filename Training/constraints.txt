# Pins for the PoSumo training venv. Pass with `-c Training/constraints.txt` on
# EVERY pip install into Training/venv, so adding one package can never drag a
# pinned one with it. CLAUDE.md is the authority on why each of these is pinned;
# the short version:
#
#   torch 2.5.1      2.6+ breaks ONNX checkpoint export. ml-agents itself allows
#                    <=2.8.0, but the export problem is ours, not theirs.
#   setuptools 69.5  70+ removes pkg_resources, which breaks mlagents-learn.
#   numpy 1.23.5     pinned by mlagents (>=1.23.5,<1.24.0).
#   onnx 1.15.0      pinned by mlagents exactly.
#   protobuf <3.21   the generated comms stubs do not load on 4.x.
#
# NEVER run `pip install --upgrade` in this venv.
torch==2.5.1
setuptools==69.5.1
numpy==1.23.5
onnx==1.15.0
protobuf==3.20.3
gymnasium==1.3.0
pettingzoo==1.26.1
tensorboard==2.20.0
